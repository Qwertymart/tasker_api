groups:
- name: docker-alerts
  rules:
  - alert: Service down
    expr: up{job=~"(auth_service|task_service|user_service|postgres)"} == 0
    for: 2m
    labels:
      severity: critical
    annotations:
      summary: "Service non-operational"
      description: "Service {{ $labels.instance }} is down."